# Helium Browser Audit — 2026-07-21

## Scope and Safety

The audit covered local and GitHub repository state, branches, history,
submodules, scripts, patches, workflows, dependencies, CI history, upstream
Helium/platform state, build capacity, and source-level sync behavior. It did
not inspect, export, copy, or decrypt a browser profile, credential database,
cookie database, session file, token, passphrase, or live secret. No build was
started, no GitHub state was changed, and nothing was pushed or deployed.

## Evidence Snapshot

| Item | Evidence |
| --- | --- |
| lm capacity | 238 GB filesystem, 19 GB free (92% used); insufficient for Chromium |
| Public repo | `dhruv9saini/helium-passwords`, public fork of `imputnet/helium-linux`, local main was clean at `9a1a86c` before audit commits |
| Private repo | `dhruv9saini/helium-sync`, private, clean at `6b6b0b6` before audit commits; full public history through `9a1a86c` already present |
| Shared layout | Existing checkout moved safely to `/home/d/coding/helium/helium-passwords`; private repo cloned beside it at `/home/d/coding/helium/helium-sync` |
| Move validation | No other worktrees, hard-coded external paths, symlinks, cron/user-systemd references, or non-audit process CWDs referenced the old path; submodule Git links are relative |
| Public branches | `main`; `push-sync` is 6 commits ahead and 47 behind with a stale sync scaffold |
| Private branches | `main`; unrelated-root `standalone-sync-legacy` retained and untouched |
| Password patch identity | Public/private autofill and UI patch files were byte-identical |
| Shared-tool drift | `build.sh` matched; private `prepare-platform.sh` and `ci-check-target.sh` had diverged to inject sync patches |
| Version drift | Checked-in Helium `0.12.4` / Chromium `148.0.7778.178`; upstream Helium `0.14.7` / Chromium `150.0.7871.128` |
| Upstream password removals | Current series includes disabled autofill crowdsourcing, removed autofill settings, mandatory password-manager disable policy, and removed password suggestion link; other UI patches remove actions/menus/importers |
| Public CI evidence | Fast injection jobs passed. The last full dispatch packaged Linux x86_64/arm64 and collected macOS arm64; Windows and macOS x86_64 later failed. It ran no password behavior test. |
| Private CI evidence | All recent jobs failed before any step. GitHub's check annotation says account payments failed or the spending limit must be raised. |
| Local checks | Public patch lint passed. All shell, Node, and Python syntax checks passed. After installing Arch Go 1.26.4, all Go tests, vet, and four command builds passed. |
| Remote capacity | Dedicated non-interactive lm-to-chromiumer SSH works. Chromiumer has 8 CPUs, 7.6 GiB RAM, no swap, about 106 GiB free on its only 119 GiB disk, no NAS mount, and no Git/Python/Docker build toolchain in the login path. Empty-job admission correctly requires 120 GiB: a 100 GiB job-tree allowance plus an independent 20 GiB operational reserve. Start admission now subtracts already-staged bytes instead of double-counting them. The current filesystem cannot pass. |
| Android device | `adb` is installed but no device was connected during audit. |

## Findings

### Backbone and builds

Helium Sync has the correct Git ancestry but not a consistently consumed build
backbone. Desktop preparation extends the Passwords wrapper, while Android
starts from raw Chromium `main` and applies only six private patches. Android is
therefore branded Chromium Sync, not a composed Helium Passwords/Helium build.
The moving default ref also makes two APKs with the same private commit
non-reproducible.

The public patches are tied to Chromium 148. Current Chromium 150 changed at
least the `PasswordSuggestionGenerator` signature, so metadata lint and
filename injection cannot establish applicability. The previous full build is
useful historical compilation evidence, not current-source or runtime evidence.

### Passwords

Public restoration changes defaults and re-adds selected native UI surfaces,
but there is no deterministic save/update/restart/autofill test. The private
native bridge exports all local credentials before its first pull and republishes
all credentials at each start. Since the daemon chooses the latest server
record, a stale device that starts later can overwrite a newer password. The
simple payload also omits much of Chromium's password schema despite code paths
already existing for `PasswordSpecificsData`.

### Cookies and login state

The CDP bridge retrieves complete Chromium cookie objects, then deliberately
normalizes them to domain/path/name plus a few flags. It drops `partitionKey`
and keys records without it. CDP documents the partition key as
`topLevelSite` plus `hasCrossSiteAncestor`; omitting it can collide two valid
cookies or restore a partitioned cookie as unpartitioned.

Symmetric upload from every device also has no safe token-rotation semantics.
A device holding an old authentication token can upload it after another device
received a replacement. Device-bound sessions are expected not to transfer:
Chrome refreshes their short-lived cookie by proving possession of a private
key on the original device.

### Tabs

`tabs` is an accepted daemon record kind and appears in design prose/tests, but
there is no browser producer, consumer, retention policy, snapshot store,
validation, or restore drill. The laptop launcher also loads an external
`tab-sync` extension whose code is not in this repository, so it cannot be the
durability authority.

### Android video and streaming

The media change sets the two expected GN flags, but no post-change Android
workflow ran and no artifact manifest proves the installed APK contains them.
Those flags do not provide Widevine. None of the six private Android patches
intentionally modifies Blink Fetch streaming or the page network stack, so the
ChatGPT symptom needs a deterministic A/B test rather than a speculative codec
or sync patch.

## Architecture Decision

The accepted architecture is in `architecture.md`; acceptance gates are in
`acceptance.md`; the actionable ledger is `../TODO.md`. In brief:

- public Passwords is the only browser/build backbone;
- private Sync merges public commits and adds overlays;
- builds consume immutable manifests and produce provenance;
- passwords reconcile before publishing local changes and use complete native
  semantics;
- cookie sync is per-domain source-to-replica, partition-aware, and treats
  device-bound sessions as reauthentication-required;
- tabs use local restore, foreign sessions, independent immutable snapshots,
  and restore drills; and
- Android media and streaming are separate patched/unpatched test matrices.

## Safe Foundational Changes Completed

- Co-located both clean repositories under one parent after path-reference and
  process checks.
- Canonicalized the public origin URL and private Git/module references to
  `dhruv9saini`.
- Added a `passwords` remote and merged the local public foundation commit into
  private history.
- Added one inherited `scripts/dev.sh` command that proves lightweight health,
  public ancestry, and password-patch identity.
- Replaced the completed Arch Desktop checklist with stable product issue IDs
  and exit criteria.
- Added the upstream runbook, target architecture, durability design, and
  desktop/Android acceptance gates.
- Added dedicated lm-to-chromiumer SSH plus a fail-closed detached build
  wrapper with cgroup CPU/memory/I/O/task/deadline limits, disk and memory
  watchdog, immutable source manifest, verified artifact return, and guarded
  cleanup. A harmless short command passed the wrapper test; no browser build
  ran.
- Installed the current signed Arch Go package and ran tests/vet/builds.

## Exact Next Actions

1. Add a local chromiumer build filesystem with at least 120 GiB available,
   retain 20 GiB free on chromiumer's root filesystem, and provision a pinned
   Nix/Docker toolchain. Do not use lm's NAS as the live workspace; rerun
   production preflight afterward.
2. Fix the GitHub billing/spend-limit block and prove one private lint+Go run;
   keep multi-hour builds manual.
3. Choose and pin one current Helium/Chromium/platform train. Generate a
   machine-readable source/artifact manifest.
4. On the builder, prepare Chromium once and run source-backed apply checks for
   public patches, desktop-safe sync patches, and Android patches.
5. Refresh Helium Passwords for the pinned Chromium source, then build the
   smallest Linux target and run the native password gate in a disposable
   profile.
6. Fix HS-001 before syncing any valuable password change between devices.
7. Replace symmetric cookie merging with the partition-aware source/replica
   design and synthetic cookie/rotation tests before treating login sync as
   reliable.
8. Implement tab layers in order: local restore invariants, independent
   snapshotter+validator+retention, restore drill, then foreign-session sync.
9. Build upstream-control and patched Android APKs from the same commit; connect
   `oneplus` and run the deterministic media and streaming matrices.
10. Only after all relevant gates pass, install an artifact on personal devices.

## Remaining Risks

- Current browser artifacts may mishandle valuable password/cookie state; the
  audit did not exercise them against real data.
- Public source drift may prevent the overlay from applying to current Helium.
- The private daemon's encrypted journal and CookieCloud file each remain a
  single durability domain without independent verified backups.
- No current Android artifact or device was available to distinguish build,
  runtime, network, codec, or site-specific failures.
- Chromiumer authentication/isolation is ready, but its disk/toolchain gate and
  private CI billing remain blocked.
