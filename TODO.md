# Helium Sync Issue Ledger

This is the private program ledger. Public password/backbone work uses the
`HP-*` IDs in `ISSUES.md`. “Source complete” never means deployable: native
compile, disposable runtime, profile backup, and device acceptance remain
separate gates.

| ID | Priority | Status | Exit criterion |
| --- | --- | --- | --- |
| HS-001 Password convergence | P0 | Canonical identity, durable serialized publication, and model tests complete; native compile/runtime open | Chromium compile plus disposable d-seed/da+oneplus pending joins prove collision-free `PasswordForm` identity migration, pull-before-publish, rapid update/delete serialization, ambiguous-success restart recovery, zero join bulk publish, zero unchanged restart writes, verified writes, tombstones, and stale conflict rejection. |
| HS-002 Whole-profile cookie model | P0 | Canonical schema-5 source/model, partition identity, bounded 32-record publication, authoritative pending-join replacement under readable rollback, and a marker-gated browser-native synthetic CookieManager transaction fixture complete; native compile/runtime open | Built disposable Sync APK passes the fixed native destination-snapshot/import/apply/readback/rejection/rollback fixture with distinct partitioned/unpartitioned identities and complete attribute coverage, then real three-client synthetic transport preserves every canonical identity/attribute, revision, and tombstone. |
| HS-003 Cookie conflicts | P0 | Synthetic conflicts/tombstones, rollback/readback, exact revision-scoped destination exceptions, same-revision suppression, unverified local-rotation hold, and native rejection/rollback fixture source complete; built destination acceptance open | Disposable browser destinations pass the native fixture, then prove production-bridge rollback, exact-cookie rejection evidence, later-revision retry, successful exception clearing, no ping-pong, and last-good local preservation. Collect an exact origin/login entry before adding a user-visible native password reauth flow; a cookie mutation or same-site DBSC inventory alone must not authorize publication. |
| HS-004 Tabs local-only durability | P0 | Three-mechanism source foundations complete: Chromium clean/crash recovery boundary, schema-2 neutral exporter/store with marked-disposable exact-topology importer/post-launch validator, and stopped compressed full-profile generations. Neutral and full-profile paths have separate producers, formats, configs, retention, two-copy receipts and restore tools. A disposable-only desktop CDP runner now proves native clean/crash/second restart, neutral import/second restart, and full-profile first/second restart; strict HMAC evidence adapters emit exactly three statuses and require both off-device replica drills. Native compile, routes, source-local schedules, desktop artifact runs and the Android sandbox adapter/device drills remain open. | Compile exporter/importer; provision independent neutral/full-profile schedules in the fixed d/da/oneplus topology; run the exact returned desktop binary through all authenticated gates; add a `computer.helium.sync.test`-only app-sandbox/CDP adapter, then independently fault-inject and run the same three mechanisms on oneplus without any personal package/profile access. |
| HS-005 Android composition | P0 | Exact Chromium 150/Helium 0.14.8 train, public source replay, and private 316-entry plan complete. The Android setup-host repair applied and GN generated 58,682 targets; the first fresh compile then exposed eight hidden GRIT workers thrashing at `memory.high` inside one Ninja edge. Central GRIT serialization is source-tested and a corrected isolated compile remains open. | The selected 301 core patches (302 upstream entries minus the password-disable patch), four Passwords patches, and eleven Sync patches apply exactly on chromiumer; GN succeeds; both native sync targets compile with returned provenance under the actual one-job policy. |
| HS-006 Android video | P0 | Fixtures, declared-codec/GN gates, explicit HLS/DASH MSE paths, artifact/device identity admission, automatic CDP Media-domain diagnostics, package-UID logcat capture, and same-source A/B receipt source complete; APK/device open | Same-source control and Sync APKs pass H.264/AAC, MSE, HLS-via-MSE, VP9/Opus and recorded AV1/Widevine expectations on oneplus; each immutable result carries browser-observable Media events and package-only logcat, then the pair verifier binds both results to one media manifest. |
| HS-007 Android streaming | P0 | Progressive HTTP/1.1/Fetch/SSE and Service Worker relay gates, artifact-carried installed-APK/package/socket runner, automatic failure evidence, source-bound A/B verifier, and live rootless tailnet-only H2/H3 origins with private-CA/SPKI admission complete. Server qlog proved Caddy/quic-go's 1280-byte Initial UDP payload exceeded Tailscale's 1280-byte interface MTU after IP/UDP headers; the pinned 1200-byte Caddy patch now passes direct H3 and delayed identity/gzip/Brotli/SSE/relay-source checks from lm and da. APK runtime remains open. | Same-source control and Sync APKs pass progressive identity/gzip/Brotli/SSE/Service Worker relay, negotiated HTTP/2/HTTP/3, background/foreground, Wi-Fi-to-cellular handoff, one fail-closed pair receipt, and content-free ChatGPT timing. |
| HS-008 Native password UX | P0 | Shared UI gate and private Sync receipt extension complete; browser execution open | Save prompt, storage, update, delete, suggestions, autofill, restart, and malformed-record gates pass on disposable desktop and Android profiles. |
| HS-009 Device credentials | P0 | Serialized supervised registry updates, scoped credential/revocation synthetic proof, cursor-atomic promotion, unfiltered mixed-inventory fixture admission, and hash-only registry source/model complete; native promotion/rotation runtime open | Per-device hash-only enrollment, pull/push scopes, revocation, overlap/confirm/retire rotation, atomic stopped-profile activation, and old-token rejection pass through built disposable clients and the supervised private-Tailnet HTTP service. |
| HS-010 Private Actions | P2 | External billing block; not on critical path | Private GitHub Actions executes again; chromiumer remains the accepted large-build path. |
| HS-011 Origin-scoped login state | P1 | Strict artifact-bound schema-2 metadata classifier and fail-closed preview/apply/readback/rollback contract complete; source adapter registry intentionally empty | Collect cookie/auth/DBSC and origin-store evidence from target sites in disposable profiles; add one reviewed exact-origin adapter only when necessary and only with isolated writers plus atomic rollback proof. Never live-merge arbitrary localStorage/IndexedDB/service-worker databases. |
| HS-012 Service recovery | P0 | Read-only server validation, private allowlisted readable backups, and temporary synthetic restore proof complete; production off-source recovery drills open | Supervised lm service has an independently verified backup/restore drill before any personal join enrollment. Client bearer tokens remain absent from lm/server/NAS backup data. |
| HS-013 Provenance | P0 | Chromium 150 Nix source pin, Android version attestation, and full-APK closure/command/composition verification complete; isolated compile/APK artifacts open | Returned native/APK artifacts include source/core/backbone/Sync commits, GN args, Nix closure, patch hashes, compile target, exact versionCode/versionName, and artifact receipt. |
| HS-014 Shared ancestry | P2 | Complete locally through public `fb5bde771a28ed14df056ae55cfae2363ffe705e` | Every private update contains the selected public Passwords commit through normal Git merge ancestry; both worktrees stay clean. |
| HS-015 Single native path | P0 | Obsolete writer/server source removed and normal composition source-tested; deployed cleanup open | Verify no previously deployed device path still invokes an old sidecar, then prove every normal launch uses only the native bridges and profile-local enrollment. |
| HS-016 Chromiumer isolation | P0 | Complete and enforced | Every large job retains two jobs, CPU/memory/I/O/task/disk caps, root floor, watchdog, eight-hour deadline, detached logs, cancellation, and exactly-once completion mail. |
| HS-017 Private Tailnet endpoint | P0 | Source and operators now require exact lm Tailscale IPv4 HTTP on port 44719, hash-only bearer authentication, no public Funnel, and no Serve listener/Web proxy/TCP forward whose parsed numeric port is 44719 while preserving unrelated tailnet-only Serve routes; malformed targets fail closed and create-new acceptance evidence enforces byte-identical before/after canonical hashes. | Prove the disposable service and client over the live Tailnet endpoint with the unrelated Serve configuration unchanged, while production remains inactive until its dedicated-account state and restore drill are ready. |
| HS-018 Profile-safe rollout | P0 | Parallel test identity, monotonic Android version, and direct NAS-plus-peer full-profile backup/receipt/restore source gates complete; device gates open | Disposable Android builds use `computer.helium.sync.test`; production is locked to versionCode `787500005`, strictly above observed installed `787500004`, with versionName `150.0.7871.181`. Each stopped personal profile must stream one compressed generation to the fixed private NAS-plus-peer topology and pass both-copy status plus a selected-destination disposable restore before install; enroll d seed, then da and oneplus one at a time; retain artifact/profile rollback until final drills pass. |
| HS-019 Bounded record transport | P0 | Go/native paged reads and native fail-fast 4 MiB request/32-cookie publication batches complete; supervised disposable service deployed; native compile/browser runtime open | A returned Chromium artifact and disposable profiles exhaust multi-page password/cookie snapshots and multiple publication batches without gaps, duplicates, cursor drift, oversized responses, partial apply, or durable cursor advancement after a failed page. |
