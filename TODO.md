# Helium Passwords Issue Ledger

This is the public product ledger. “Source complete” never means deployable: native
compile, disposable runtime, profile backup, and device acceptance remain
separate gates.

| ID | Priority | Status | Exit criterion |
| --- | --- | --- | --- |
| HS-001 Password convergence | P0 | Canonical identity, durable serialized publication, and model tests complete; native compile/runtime open | Chromium compile plus disposable d-seed/da+oneplus pending joins prove collision-free `PasswordForm` identity migration, pull-before-publish, rapid update/delete serialization, ambiguous-success restart recovery, zero join bulk publish, zero unchanged restart writes, verified writes, tombstones, and stale conflict rejection. |
| HS-002 Cookie backup boundary | P0 | Normal browser and server paths are password-only. A marked-disposable native CookieManager snapshot/import/readback/rollback fixture is complete; native compile/runtime remains open. | Built disposable artifacts pass C1 neutral CookieManager restore, C2 stopped compressed-profile restore, and C3 independently encrypted restic-profile restore with synthetic cookies only. No personal Google cookie is read. |
| HS-003 Cookie restore failure handling | P0 | Neutral batch rejection rolls back to the destination snapshot; compressed archives quarantine invalid generations; restic checks authenticated repository data before and after restore. | Disposable restores prove rejected native apply rollback, corrupt compressed-generation quarantine, damaged restic data detection, and last-good recovery without any live cookie replication. |
| HS-004 Tabs local-only durability | P0 | Three-mechanism source foundations complete: Chromium clean/crash recovery boundary, schema-2 neutral exporter/store with marked-disposable exact-topology importer/post-launch validator, and stopped compressed full-profile generations. Neutral and full-profile paths have separate producers, formats, configs, retention, two-copy receipts and restore tools; selected full-profile restore can recover from one healthy replica after its sibling is quarantined. The HMAC-authenticated runner covers desktop and an exact-APK `computer.helium.sync.test`-only Android app-sandbox/CDP adapter. A guarded `.test`-only phase reset and fleet full-E2E finalizer bind the exact Linux deployment/internal/full-graph provenance, both Android archives, five normal proofs, three emitted status receipts, and two fault-recovery proofs for each of d, da, and OnePlus. Fixed d/da/oneplus routes, current `helium-tabs` tools, and fail-closed inactive scheduler staging are deployed; no timer is enabled without a fresh native export. Native artifact runs and real device drills remain open. | Compile exporter/importer in the returned artifacts; run the sole fleet integration gate through both replica destinations and both damaged-generation drills without any personal package/profile access; then enable each source schedule only after its first complete two-destination generation passes. |
| HS-005 Android composition | P0 | Exact Chromium 150/Helium 0.14.8 train, public source replay, and private 321-entry plan complete. The Android setup-host repair applied and GN generated 58,682 targets; the first fresh compile exposed eight hidden GRIT workers thrashing at `memory.high` inside one Ninja edge, and the retained Linux compile later exposed ThinLTO selecting all eight backends inside one link and rereading more than 5.5 TiB under the 4 GiB memory-high boundary. GRIT and ThinLTO serialization are source-tested; a corrected isolated compile remains open. | The selected 301 core patches (302 upstream entries minus the password-disable patch), seven Passwords patches, and thirteen Sync patches apply exactly on chromiumer; GN succeeds; both native sync targets compile with returned provenance under the actual one-job policy. |
| HS-006 Android video | P0 | Fixtures, declared-codec/GN gates, explicit HLS/DASH MSE paths, artifact/device identity admission, automatic CDP Media-domain diagnostics, package-UID logcat capture, and same-source A/B receipt source complete; APK/device open | Same-source control and Sync APKs pass H.264/AAC, MSE, HLS-via-MSE, VP9/Opus and recorded AV1/Widevine expectations on oneplus; each immutable result carries browser-observable Media events and package-only logcat, then the pair verifier binds both results to one media manifest. |
| HS-007 Android streaming | P0 | Progressive HTTP/1.1/Fetch/SSE and Service Worker relay gates, artifact-carried installed-APK/package/socket runner, automatic failure evidence, source-bound A/B verifier, and live rootless tailnet-only H2/H3 origins with private-CA/SPKI admission complete. Server qlog proved Caddy/quic-go's 1280-byte Initial UDP payload exceeded Tailscale's 1280-byte interface MTU after IP/UDP headers; the pinned 1200-byte Caddy patch now passes direct H3 and delayed identity/gzip/Brotli/SSE/relay-source checks from lm and da. APK runtime remains open. | Same-source control and Sync APKs pass progressive identity/gzip/Brotli/SSE/Service Worker relay, negotiated HTTP/2/HTTP/3, background/foreground, Wi-Fi-to-cellular handoff, one fail-closed pair receipt, and content-free ChatGPT timing. |
| HS-008 Native password UX | P0 | Shared UI gate and private Sync receipt extension complete; browser execution open | Save prompt, storage, update, delete, suggestions, autofill, restart, and malformed-record gates pass on disposable desktop and Android profiles. |
| HS-009 Device credentials | P0 | Serialized supervised registry updates, scoped credential/revocation synthetic proof, cursor-atomic promotion, unfiltered mixed-inventory fixture admission, and hash-only registry source/model complete; native promotion/rotation runtime open | Per-device hash-only enrollment, pull/push scopes, revocation, overlap/confirm/retire rotation, atomic stopped-profile activation, and old-token rejection pass through built disposable clients and the supervised private-Tailnet HTTP service. |
| HS-010 Private Actions | P2 | External billing block; not on critical path | Private GitHub Actions executes again; chromiumer remains the accepted large-build path. |
| HS-011 Origin-scoped login state | P1 | Strict artifact-bound metadata classifier exists; source adapter registry is intentionally empty | Keep localStorage, IndexedDB, service-worker databases, Cache Storage, and arbitrary site state out of Helium Passwords. |
| HS-012 Service recovery | P0 | Read-only server validation, private allowlisted readable backups, and a live stopped-service disposable backup/temporary-restore proof on lm NAS complete; production off-source recovery drills open | Keep the verified disposable archive timer healthy. Before any personal join enrollment, run the corresponding supervised production restore drill; client bearer tokens must remain absent from lm/server/NAS backup data. |
| HS-013 Provenance | P0 | Chromium 150 Nix source pin, Android version attestation, full-APK closure/command/composition verification, and independent product/runtime-kit identity binding complete; isolated compile/APK artifacts open | Returned native/APK artifacts include source/core/backbone/Sync commits, the separately locked accepted runtime-kit commit and byte inventory, GN args, Nix closure, patch hashes, compile target, exact versionCode/versionName, and distinct job-scoped artifact receipts for same-basename test/production archives. |
| HS-014 Public source consolidation | P0 | Implementation history is consolidated in `github.com/dhruv9saini/helium-passwords`; the production branch is pushed. | Merge verified source and evidence to the public default branch, then remove the obsolete private remote and local checkout only after proving no unique unpushed commits or artifacts remain. |
| HS-015 Single native path | P0 | Obsolete writer/server source removed and normal composition source-tested; deployed cleanup open | Verify no previously deployed device path still invokes an old sidecar, then prove every normal launch uses only the native bridges and profile-local enrollment. |
| HS-016 Chromiumer isolation | P0 | Complete and enforced | Every large job retains the bounded build and watchdog units, CPU/memory/I/O/task/disk caps, root floor, hard deadline, detached logs, cancellation, and one protected local terminal result. Monitoring never queues, emails, relays, or sends a notification. |
| HS-017 Private Tailnet endpoint | P0 | Source and operators require exact lm Tailscale IPv4 HTTP on port 44719, hash-only bearer authentication, no public Funnel, and no Serve listener/Web proxy/TCP forward whose parsed numeric port is 44719 while preserving unrelated tailnet-only Serve routes. The supervised disposable service is live at `100.100.105.47:44719`; exposure verification passes, and the physical OnePlus Android shell reaches `/v2/health` through the Tailnet. | Bind the returned desktop and Android `.test` artifacts to the live disposable endpoint and preserve the unchanged unrelated Serve proof. Production remains inactive until its dedicated-account state and restore drill are ready. |
| HS-018 Profile-safe rollout | P0 | Parallel test identity, monotonic Android version, and direct NAS-plus-peer full-profile backup/receipt/restore source gates complete; device gates open | Disposable Android builds use `computer.helium.sync.test`; production is locked to versionCode `787500005`, strictly above observed installed `787500004`, with versionName `150.0.7871.181`. Each stopped personal profile must stream one compressed generation to the fixed private NAS-plus-peer topology and pass both-copy status plus a selected-destination disposable restore before install; enroll d seed, then da and oneplus one at a time; retain artifact/profile rollback until final drills pass. |
| HS-019 Bounded record transport | P0 | Go/native paged password reads and a fail-fast 4 MiB native request ceiling are complete; the server rejects every non-password kind. | Returned artifacts exhaust multi-page password snapshots without gaps, duplicates, cursor drift, oversized responses, partial apply, or durable cursor advancement after a failed page. |
| HS-020 Nine independent recovery paths | P0 | Source fixes exactly three materially different mechanisms per data type. Passwords use Tailnet journal replay, stopped compressed profile, and native neutral PasswordStore snapshots. Cookies use native neutral CookieManager snapshots, stopped compressed profile, and an independently encrypted restic repository. Tabs use native session recovery, neutral topology generations, and stopped compressed profile. Exact storage, encryption, integrity, retention, and restore behavior are documented. | Perform all nine restores on marked disposable profiles with exact returned Linux/Android artifacts, including corrupt-input drills and content-free browser readback receipts, before personal OnePlus enrollment. |
