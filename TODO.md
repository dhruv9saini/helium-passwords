# Helium Sync Issue Ledger

This is the private program ledger. Public password/backbone work uses the
`HP-*` IDs in `ISSUES.md`. “Source complete” never means deployable: native
compile, disposable runtime, profile backup, and device acceptance remain
separate gates.

| ID | Priority | Status | Exit criterion |
| --- | --- | --- | --- |
| HS-001 Password convergence | P0 | Source complete; native compile/runtime open | Chromium compile plus disposable d-seed/da+oneplus pending joins prove pull-before-publish, zero join bulk publish, zero unchanged restart writes, verified writes, tombstones, and stale conflict rejection. |
| HS-002 Whole-profile cookie model | P0 | Source/model and supervised da/oneplus TLS proof complete; native compile/runtime open | Built CookieManager round-trip preserves every canonical identity/attribute, partition key, session cookie, revision, and tombstone. |
| HS-003 Cookie rotation/conflicts | P0 | Supervised synthetic rotations/conflicts/tombstones and model rollback complete; native destination acceptance open | Disposable browser destinations prove sealed rollback, CookieManager readback, rejection recovery, DBSC classification, no ping-pong, and last-good local preservation. |
| HS-004 Tabs local-only durability | P0 | Checked store/backup, independently validated neutral restore, config-independent pinned transport, and fail-closed OnePlus `/dev` invariant complete; browser/device open | Native exporter compiles; authorize da's dedicated key on d; provision two independent recovery recipients per source; then each device independently proves fresh generations, two encrypted off-source copies, corruption quarantine, retention health, and a disposable browser restore. |
| HS-005 Android composition | P0 | Pinned source workflow running on chromiumer | All 292 patches apply exactly, GN succeeds, and both native sync targets compile from the pinned source with returned provenance. |
| HS-006 Android video | P0 | Fixtures/GN checks complete; APK/device open | Same-SHA control and Sync APKs pass H.264/AAC, MSE, VP9/Opus and recorded AV1/Widevine expectations on oneplus. |
| HS-007 Android streaming | P0 | HTTP/1.1/SSE fixtures complete; protocol/APK open | Same-SHA control and Sync APKs pass progressive identity/gzip/Brotli/SSE plus HTTP/2, HTTP/3, background/foreground, handoff, and content-free ChatGPT timing. |
| HS-008 Native password UX | P0 | Shared UI gate and private Sync receipt extension complete; browser execution open | Save prompt, storage, update, delete, suggestions, autofill, restart, and malformed-record gates pass on disposable desktop and Android profiles. |
| HS-009 Device credentials | P0 | Supervised synthetic enrollment/revocation and Android CLI complete; native promotion/rotation open | Per-device hash-only enrollment, pull/push scopes, revocation, overlap/confirm/retire rotation, atomic stopped-profile activation, and old-token rejection pass through the supervised TLS service. |
| HS-010 Private Actions | P2 | External billing block; not on critical path | Private GitHub Actions executes again; chromiumer remains the accepted large-build path. |
| HS-011 Origin-scoped login state | P1 | Strict artifact-bound metadata classifier complete; browser collection/adapters open | Collect cookie/auth/DBSC and origin-store evidence from target sites in disposable profiles; add only evidenced per-origin export/import adapters. Never live-merge arbitrary localStorage/IndexedDB/service-worker databases. |
| HS-012 Opaque service recovery | P0 | Journal and encrypted d recovery source/tests complete; deployment proof open | Supervised lm service has an independently verified opaque backup/restore drill; both dedicated recovery identities restore their off-source encrypted d generation into disposable state before any join enrollment. Recovery identities remain absent from lm/server/NAS data. |
| HS-013 Provenance | P0 | Pinned Nix environment realized and returned; compile artifact open | Returned native/APK artifacts include source/core/backbone/Sync commits, GN args, Nix closure, patch hashes, compile target, and artifact receipt. |
| HS-014 Shared ancestry | P2 | Complete locally | Every private update contains the selected public Passwords commit through normal Git merge ancestry; both worktrees stay clean. |
| HS-015 Single native path | P0 | Obsolete writer/server source removed and normal composition source-tested; deployed cleanup open | Verify no previously deployed device path still invokes an old sidecar, then prove every normal launch uses only the native bridges and profile-local enrollment. |
| HS-016 Chromiumer isolation | P0 | Complete and enforced | Every large job retains two jobs, CPU/memory/I/O/task/disk caps, root floor, watchdog, eight-hour deadline, detached logs, cancellation, and exactly-once completion mail. |
| HS-017 TLS endpoint | P0 | Direct TLS source/issuance/install gates complete; deployment proof open | Create the constrained CA on offline media, install the verified lm leaf while inactive, explicitly enroll the public root on disposable d/da/oneplus, then prove TLS 1.3 health and wrong-root rejection over the tailnet address. Serve/Funnel remain empty. |
| HS-018 Profile-safe rollout | P0 | Parallel Android test identity source complete; device gates open | Disposable Android builds use `computer.helium.sync.test` and cannot replace the personal app. Hash-verified recoverable profile backups still precede production install; enroll d seed, then da and oneplus one at a time; retain artifact/profile rollback until final drills pass. |
