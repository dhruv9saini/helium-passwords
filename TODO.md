# Helium Sync Issue Ledger

This is the private program ledger. Public password/backbone work uses the
`HP-*` IDs in `ISSUES.md`. “Source complete” never means deployable: native
compile, disposable runtime, profile backup, and device acceptance remain
separate gates.

| ID | Priority | Status | Exit criterion |
| --- | --- | --- | --- |
| HS-001 Password convergence | P0 | Canonical identity, durable serialized publication, and model tests complete; native compile/runtime open | Chromium compile plus disposable d-seed/da+oneplus pending joins prove collision-free `PasswordForm` identity migration, pull-before-publish, rapid update/delete serialization, ambiguous-success restart recovery, zero join bulk publish, zero unchanged restart writes, verified writes, tombstones, and stale conflict rejection. |
| HS-002 Whole-profile cookie model | P0 | Canonical v3 source/model, partition identity, active-epoch rekey, and supervised da/oneplus TLS proof complete; native compile/runtime open | Built CookieManager round-trip preserves every canonical identity/attribute, partition key, session cookie, revision, tombstone, and accepts only higher-revision active-epoch rekeys. |
| HS-003 Cookie rotation/conflicts | P0 | Synthetic rotations/conflicts/tombstones, rollback/readback, and exact revision-scoped destination-exception retry model complete; native destination acceptance open | Disposable browser destinations prove sealed rollback, exact-cookie rejection evidence, same-revision suppression, later active-revision/local-reauth retry, successful exception clearing, no ping-pong, and last-good local preservation without claiming DBSC authentication. |
| HS-004 Tabs local-only durability | P0 | Checked store/backup, independently validated neutral restore, atomic marked-root current-URL browser consumer, config-independent pinned transport, and fail-closed OnePlus `/dev` invariant complete; full browser/device integration open | Native exporter compiles; authorize da's dedicated key on d; provision two independent recovery recipients per source; then each device independently proves fresh generations, two encrypted off-source copies, corruption quarantine, retention health, full tab-topology reconstruction, and first/second disposable browser starts. |
| HS-005 Android composition | P0 | Exact Chromium 150/Helium 0.14.8 train, public source replay, and private 310-entry plan complete; isolated full apply/compile open | The selected 301 core patches (302 upstream entries minus the password-disable patch), two Passwords patches, and seven Sync patches apply exactly on chromiumer; GN succeeds; both native sync targets compile with returned provenance. |
| HS-006 Android video | P0 | Fixtures, declared-codec/GN gates, explicit HLS/DASH MSE paths, and artifact/device identity admission complete; APK/device open | Same-SHA control and Sync APKs pass H.264/AAC, MSE, VP9/Opus and recorded AV1/Widevine expectations on oneplus. |
| HS-007 Android streaming | P0 | HTTP/1.1/SSE fixtures, artifact-carried installed-APK/package/socket runner, and live rootless tailnet-only H2/H3 origins with private-CA/SPKI admission and lm restart proof complete; da direct H3/APK runtime open | Resolve da's QUIC client timeout, then same-SHA control and Sync APKs pass progressive identity/gzip/Brotli/SSE, negotiated HTTP/2/HTTP/3, background/foreground, Wi-Fi-to-cellular handoff, and content-free ChatGPT timing. |
| HS-008 Native password UX | P0 | Shared UI gate and private Sync receipt extension complete; browser execution open | Save prompt, storage, update, delete, suggestions, autofill, restart, and malformed-record gates pass on disposable desktop and Android profiles. |
| HS-009 Device credentials | P0 | Serialized supervised registry updates, scoped credential/revocation synthetic proof, cursor-atomic promotion, unfiltered mixed-inventory fixture admission, and native cookie active-epoch transition source/model complete; native promotion/rotation runtime open | Per-device hash-only enrollment, pull/push scopes, revocation, overlap/confirm/retire rotation, atomic stopped-profile activation, active-epoch cookie CAS rekey, stale/unknown epoch rejection, and old-token rejection pass through built disposable clients and the supervised TLS service. |
| HS-010 Private Actions | P2 | External billing block; not on critical path | Private GitHub Actions executes again; chromiumer remains the accepted large-build path. |
| HS-011 Origin-scoped login state | P1 | Strict artifact-bound metadata classifier complete; browser collection/adapters open | Collect cookie/auth/DBSC and origin-store evidence from target sites in disposable profiles; add only evidenced per-origin export/import adapters. Never live-merge arbitrary localStorage/IndexedDB/service-worker databases. |
| HS-012 Opaque service recovery | P0 | Read-only server validation, private allowlisted opaque backups, and two-recipient temporary synthetic restore proof complete; production and independently held off-source recovery drills open | Supervised lm service has an independently verified opaque backup/restore drill; both dedicated recovery identities restore their off-source encrypted d generation into disposable state before any personal join enrollment. Recovery identities remain absent from lm/server/NAS data. |
| HS-013 Provenance | P0 | Chromium 150 Nix source pin, Android version attestation, and full-APK closure/command/composition verification complete; isolated compile/APK artifacts open | Returned native/APK artifacts include source/core/backbone/Sync commits, GN args, Nix closure, patch hashes, compile target, exact versionCode/versionName, and artifact receipt. |
| HS-014 Shared ancestry | P2 | Complete locally through public `00d27ec` | Every private update contains the selected public Passwords commit through normal Git merge ancestry; both worktrees stay clean. |
| HS-015 Single native path | P0 | Obsolete writer/server source removed and normal composition source-tested; deployed cleanup open | Verify no previously deployed device path still invokes an old sidecar, then prove every normal launch uses only the native bridges and profile-local enrollment. |
| HS-016 Chromiumer isolation | P0 | Complete and enforced | Every large job retains two jobs, CPU/memory/I/O/task/disk caps, root floor, watchdog, eight-hour deadline, detached logs, cancellation, and exactly-once completion mail. |
| HS-017 TLS endpoint | P0 | Direct TLS source/issuance/install gates and live synthetic TLS 1.3 tailnet health proof complete; dedicated-account production deployment open | Create the constrained CA on offline media, install the verified lm leaf while inactive, explicitly enroll the public root on disposable d/da/oneplus, then prove TLS 1.3 health and wrong-root rejection over the tailnet address. Serve/Funnel remain empty. |
| HS-018 Profile-safe rollout | P0 | Parallel test identity plus monotonic Android version source gates complete; device gates open | Disposable Android builds use `computer.helium.sync.test`; production is locked to versionCode `787500005`, strictly above observed installed `787500004`, with versionName `150.0.7871.181`. Hash-verified recoverable profile backups still precede production install; enroll d seed, then da and oneplus one at a time; retain artifact/profile rollback until final drills pass. |
| HS-019 Bounded record transport | P0 | Go/native source complete and supervised disposable service deployed; native compile/browser runtime open | A returned Chromium artifact and disposable profiles exhaust multi-page password/cookie snapshots without gaps, duplicates, cursor drift, oversized responses, partial apply, or durable cursor advancement after a failed page. |
