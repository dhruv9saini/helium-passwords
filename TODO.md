# Helium Sync Issues

This is the canonical private product ledger. Public backbone issues use the
stable `HP-*` IDs in [ISSUES.md](ISSUES.md). Do not close an issue from patch
lint, overlay injection, or a successful compile alone.

| ID | Priority | Status | Issue | Exit criterion |
| --- | --- | --- | --- | --- |
| HS-001 | P0 | Code complete; needs browser build/device | Native and CDP startup now persist hash/server-sequence baselines, pull and apply newer remote generations first, and publish only local hash changes. Synthetic restart, stale-device, offline-edit, corrupt-state, and migration tests pass. | Compile the overlay, then run the two-device disposable-profile offline/change/reconnect gate and prove browser restarts emit zero records. |
| HS-002 | P0 | CDP code complete; native integration open | Cookie schema v2 preserves unpartitioned and CHIPS identity, including `topLevelSite`, `hasCrossSiteAncestor`, source scheme, and source port. Synthetic collision and legacy migration tests pass. | Run the fixture matrix against the pinned Android and desktop CDP versions, then replace CDP with a native CookieManager producer/consumer where Android background reliability requires it. |
| HS-003 | P0 | CDP code complete; deployment open | Every configured domain has one source, named replicas, a monotonic generation, and `portable` or `device-bound` mode. Replica code has no upload path; the local server uses atomic revision compare-and-swap; synthetic rotation/conflict/reauthentication tests pass. | Migrate the private config without reading live values, run source/replica fixture tests on all devices, and replace legacy unauthenticated CookieCloud encryption under HS-011. |
| HS-004 | P0 | Snapshot store complete; browser integration open | `internal/tabsnapshot` and `helium-tabs` atomically create and validate independent generations, fail retention closed, preserve protected/invalid copies, and restore only to new disposable state. Synthetic corruption/retention/restore tests pass. | Implement the Chromium tab/session producer and disposable-profile consumer, schedule captures, record restore drills, and complete the local-session plus foreign-session layers. |
| HS-005 | P0 | Open | The Android build starts from raw moving Chromium and applies six private patches. It does not consume the Helium Passwords/Helium common patch train. | Android build inputs name one pinned public backbone and Chromium commit, the composed patch set is source-checked, and artifact provenance proves what was built. |
| HS-006 | P0 | Needs build/device | Android video failure is not reproduced by a deterministic codec matrix. `ffmpeg_branding="Chrome"` and `proprietary_codecs=true` are configured but unvalidated. | Upstream-control and Helium Sync APKs from the same Chromium SHA pass/fail a recorded MP4/H.264/AAC, WebM/VP9/Opus, AV1, MSE, and DRM-expected-failure matrix. |
| HS-007 | P0 | Needs build/device | ChatGPT-style response streaming has no deterministic reproduction and none of the six Android patches intentionally changes Blink or page networking. | Timed HTTP/1.1 chunked, HTTP/2, HTTP/3, SSE, and Fetch `ReadableStream` fixtures record first-byte/chunk timing; patched/unpatched APK A/B identifies the failing layer. |
| HS-008 | P1 | Open | Password payload `helium-password-v1` discards many Chromium fields and bypasses Chromium's mature password sync merge semantics. | The schema is versioned from `PasswordSpecificsData` or equivalently complete, validated, migration-tested, and conflict behavior matches native password semantics. |
| HS-009 | P1 | Open | The sync bearer token is loaded once, has no key ID, overlap window, revocation list, or rotation command. | Rotation supports current+next tokens for a bounded window, atomically updates clients, revokes the old token, and has a recovery drill. |
| HS-010 | P1 | Blocked external | Every private Actions job is rejected before its first step because of the GitHub account billing/spend limit. | A new private Go and lint run executes steps and passes; expensive Chromium workflows remain manual. |
| HS-011 | P1 | Open; atomic CAS complete | CookieCloud still uses legacy MD5-derived AES-CBC without authentication and has no independent backup generations. The server now atomically replaces a revisioned record and rejects stale/unconditional writes. | Sensitive cookie records use an authenticated format, independent backup generations, and corruption/restore tests. |
| HS-012 | P1 | Code complete; operational drill open | The encrypted journal now checkpoints authenticated, hashed generations by fsync+rename after durable writes. Startup authenticates every record, quarantines a malformed journal, skips corrupt snapshot generations, and restores the newest valid copy. Eight valid generations are retained while invalid evidence is preserved; synthetic wrong-key, corrupt-tail, corrupt-newest, retention, and no-plaintext tests pass. | Run a stop/corrupt/restart/recover drill on a synthetic daemon data directory, archive one encrypted generation off-host, and record the result without exposing payloads. |
| HS-013 | P1 | Open | Artifact builds do not emit a complete machine-readable provenance manifest or run product acceptance tests. | Every artifact records commits, Chromium/Helium versions, GN args, patch hashes, toolchain, target, and artifact hash before testing. |
| HS-014 | P2 | Complete locally | Both repositories are under `/home/d/coding/helium`, canonical URLs are used, and the shared lightweight command proves Passwords ancestry and patch identity. | Preserve this invariant in every backbone update. |
| HS-015 | P2 | Open | Android/Arch device setup concerns dominate `AGENTS.md` and obscure browser product invariants. | Device instructions remain accurate but product architecture, issue IDs, commands, and acceptance gates are the first references contributors encounter. |
| HS-016 | P0 | Blocked capacity | Dedicated lm-to-chromiumer SSH and the isolated build wrapper pass a harmless test, but chromiumer has only about 106 GiB free against the enforced 120 GiB threshold and lacks Git/Python/Docker build tooling in its login environment. | A local chromiumer build filesystem and pinned toolchain make production `scripts/chromiumer-job.sh preflight` pass; no limit is weakened and no live build uses lm or the NAS. |
