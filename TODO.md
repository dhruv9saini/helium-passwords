# Helium Sync Issues

This is the canonical private product ledger. Public backbone issues use the
stable `HP-*` IDs in [ISSUES.md](ISSUES.md). Do not close an issue from patch
lint, overlay injection, or a successful compile alone.

| ID | Priority | Status | Issue | Exit criterion |
| --- | --- | --- | --- | --- |
| HS-001 | P0 | Code complete; needs browser build/device | Native and CDP startup now persist hash/server-sequence baselines, pull and apply newer remote generations first, and publish only local hash changes. Synthetic restart, stale-device, offline-edit, corrupt-state, and migration tests pass. | Compile the overlay, then run the two-device disposable-profile offline/change/reconnect gate and prove browser restarts emit zero records. |
| HS-002 | P0 | Native/CDP code complete; build/device open | Both paths preserve unpartitioned and CHIPS identity, host/domain form, source scheme/port, attributes, and fail-closed migration. The native bridge uses Chromium's privileged CookieManager; source tests and synthetic collision tests pass. | Compile against pinned Chromium, reconcile the native/CDP wire encodings, then run synthetic CookieManager fixtures on Android and desktop. |
| HS-003 | P0 | Native/CDP authority code complete; deployment/auth open | Every domain has one source, replicas never publish, portable rotations are versioned, and device-bound policies copy no value. Native and CDP code pull first; the local server atomically compare-and-swaps revisions. | Migrate private policy without reading values, run real source/replica fixture tests, add server-side device/domain authority, and replace legacy CookieCloud encryption under HS-011. |
| HS-004 | P0 | Store and native producer code complete; runtime integration open | The Chromium bridge atomically exports the bounded Go schema outside the profile. `helium-tabs` commits/validates independent generations, fails retention closed, and restores only to disposable state. Synthetic source, corruption, retention, and restore tests pass. | Compile the bridge; add Android unloaded-tab export, schedule export-to-generation capture, implement a disposable browser consumer plus foreign sessions, and record restore drills. |
| HS-005 | P0 | Composition code complete; full source gate open | `chromium/android-build.lock` pins Chromium 148 and Helium core. One script composes 284 ordered core patches (omitting only password-disable), two Passwords patches, six Sync patches/overlay, transforms/resources, strict GN args, and provenance. Contract tests pass. | On chromiumer, prepare the complete checkout and prove all 292 patches apply with zero fuzz; generate GN and verify the emitted provenance before compilation. |
| HS-006 | P0 | Fixture/config code complete; APK/device open | Reproducible H.264/AAC MP4, fragmented MSE MP4, and VP9/Opus WebM fixtures, ranges, capability probes, playback/frame/audio observations, and fail-closed codec GN/provenance checks exist. | Build upstream-control and Sync APKs from the same SHA, run the recorded matrix on oneplus, then add AV1 and explicit Widevine expected-failure coverage. |
| HS-007 | P0 | HTTP/1.1/SSE fixture complete; protocol/APK open | The loopback fixture proves progressive identity/gzip/Brotli Fetch chunks, SSE order, UI progress, and disposable-CDP result validation. None of the six Sync patches intentionally changes Blink/networking. | Add HTTP/2 and HTTP/3 fixtures, then A/B upstream-control and Sync APKs through background/foreground and network handoff before the manual ChatGPT timing scenario. |
| HS-008 | P1 | Native schema code complete; build/device open | Native output now uses Chromium's complete serialized `PasswordSpecificsData`; `helium-password-v1` is read-only migration input. Remote payload identity and one-add-or-update behavior are source-tested. | Compile against pinned Chromium and run disposable-profile legacy migration, conflict, save/update/restart/autofill tests; design safe retirement before changing stable record keys. |
| HS-009 | P1 | Open | The sync bearer token is loaded once, has no key ID, overlap window, revocation list, or rotation command. | Rotation supports current+next tokens for a bounded window, atomically updates clients, revokes the old token, and has a recovery drill. |
| HS-010 | P1 | Blocked external | Every private Actions job is rejected before its first step because of the GitHub account billing/spend limit. | A new private Go and lint run executes steps and passes; expensive Chromium workflows remain manual. |
| HS-011 | P1 | Open; atomic CAS complete | CookieCloud still uses legacy MD5-derived AES-CBC without authentication and has no independent backup generations. The server now atomically replaces a revisioned record and rejects stale/unconditional writes. | Sensitive cookie records use an authenticated format, independent backup generations, and corruption/restore tests. |
| HS-012 | P1 | Code and synthetic daemon drill complete; off-host copy open | The encrypted journal checkpoints authenticated, hashed fsync+rename generations. Startup authenticates records, quarantines malformed journals, skips corrupt generations, and restores the newest valid copy. Eight valid generations are retained; unit tests and a disposable stop/corrupt/restart daemon drill pass without plaintext output. | Archive and verify one encrypted generation off-host and record a periodic recovery drill. |
| HS-013 | P1 | Android provenance code complete; artifact/desktop open | Android preparation records exact source/core/Sync commits, resolved GN args, ordered patch hashes, composition order, and aggregate provenance, then packages it with artifacts. | Validate an actual Android artifact on chromiumer, add toolchain/container identity and artifact hash receipt, and extend the same manifest to desktop artifacts. |
| HS-014 | P2 | Complete locally | Both repositories are under `/home/d/coding/helium`, canonical URLs are used, and the shared lightweight command proves Passwords ancestry and patch identity. | Preserve this invariant in every backbone update. |
| HS-015 | P2 | Open | Android/Arch device setup concerns dominate `AGENTS.md` and obscure browser product invariants. | Device instructions remain accurate but product architecture, issue IDs, commands, and acceptance gates are the first references contributors encounter. |
| HS-016 | P0 | Blocked physical capacity/toolchain | Dedicated SSH and the corrected isolated wrapper pass a harmless detached test. Empty-job admission is correctly 100 GiB job allowance + 20 GiB reserve; start subtracts staged use. Chromiumer's whole 116 GiB root filesystem has only about 106 GiB free, so it cannot pass, and Git/Python/build tooling are absent. | Add a local build filesystem with 120 GiB available while retaining 20 GiB on `/`, provision the pinned toolchain, and make production preflight pass without weakening limits or using lm/NAS as a live workspace. |
