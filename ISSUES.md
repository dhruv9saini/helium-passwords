# Helium Passwords Issues

This is the public issue ledger. IDs are stable so private Helium Sync work can
refer to the same backbone problems without publishing private implementation
details.

| ID | Priority | Status | Issue | Exit criterion |
| --- | --- | --- | --- | --- |
| HP-001 | P0 | Exact train and source replay complete; compile open | Helium `81bb0219ad6df2adefd12f42ca79198f049f1497`, Chromium `24b04c927b23c39cf9c5227cc8dc6f64a744c8e9` (`150.0.7871.181`), and Linux platform `9fbdff55283c9275f285c49dc054a1ff38dcdc96` are pinned. Both Passwords patches apply after the ordered 302-patch core series. | Linux compiles under the constrained chromiumer workflow and the manifest records all commits, args, and hashes. |
| HP-002 | P0 | Executable gate complete; artifact execution open | The artifact-bound protocol now enforces the ordered native settings/save/storage/suggestion/autofill/generation/update/delete lifecycle with immutable screenshots and independent loopback credential observations. No compiled artifact has produced a receipt. | The disposable-profile gate in `docs/password-runtime-acceptance.md` passes on Linux and the platform-specific subset passes on macOS and Windows. |
| HP-003 | P0 | Chromium 150 source replay complete; browser validation open | The two restoration patches are refreshed against the actual Chromium-150-plus-Helium series. The port follows Chromium 150's current page-action architecture and restores the second importer path that Helium strips. | Compile the selected train and run HP-002 against its artifact. |
| HP-004 | P1 | Local Chromium 150 source gate complete; CI wiring open | `scripts/check-password-patch-stack.sh` fetches only the 15 affected official Chromium 150 files, replays the ordered Helium patches, applies both restoration patches, and asserts the restored policy/UI/importer surfaces. | Run the same source-backed gate in CI before any multi-hour build. |
| HP-005 | P1 | Open | The last full matrix did not complete Windows or macOS x86_64. | All six desktop artifact jobs finish or unsupported targets are explicitly removed from the matrix. |
| HP-006 | P1 | Current Linux lock complete; macOS/Windows open | Linux fetches and checks out exact full commit `9fbdff55283c9275f285c49dc054a1ff38dcdc96` even though no `0.14.8.1` tag exists; artifact manifests carry that actual commit. macOS and Windows still permit moving refs. | Pin and verify full platform commits for macOS and Windows, then include platform/core commits, Chromium version, GN args, patch hashes, and artifact hashes in every platform artifact. |
| HP-007 | P2 | Ready locally | Repositories and developer commands were fragmented. | Both repos live under `/home/d/coding/helium`, share `scripts/dev.sh`, and Sync contains Passwords `main` through Git ancestry. |
| HP-008 | P2 | Executable protocol complete; browser execution open | The stateful loopback fixture compares synthetic login/update/autofill observations only as in-memory hashes and emits secret-free evidence after deletion. The artifact-bound harness admits only a disposable Linux profile or metadata-proven Android `.test` package and has source tests. | A built browser completes the native sequence and produces a verified receipt without real credentials, profiles, extensions, CDP password writers, or direct database access. |
| HP-009 | P0 | Disk admission and Chromium 150 Nix source pin ready; realization and compile open | Chromiumer SSH and isolation are ready. Jobs declare their own disk budgets; the Nix expression now follows Chromium `150.0.7871.181`'s exact nixpkgs pin. | Realize and record the pinned Nix environment through its isolated admission job, rerun `scripts/chromiumer-job.sh preflight 80`, complete the bounded source/compile proof, and record artifact provenance. |
| HP-010 | P1 | Complete locally | Chromiumer terminal state is observed by a persistent lm systemd timer and idempotently queued through Codex Mailbridge for success, failure, timeout, or cancellation. | Source simulations cover all terminal results plus a temporary queue failure; constrained job `hp-notify-accept-20260721-1829` reached Mailbridge Sent once with one SMTP attempt and one exact Message-ID match; the timer remains enabled across lm restart. |
| HP-011 | P0 | Public recovery and remote wrapper proof complete; downstream merge open | Android job `hs-android-148-disposable-apk-06` exposed a synchronous watchdog scan racing a Git checkout and a redundant moving-main/two-sync source flow. | GNU find deletion races do not kill the watcher; root and two-sample memory guards remain one-second fail-closed during a slow scan; real scan failures and disk overruns stop the build; source acquisition makes one shallow `gclient sync --revision src@<locked SHA>` request; focused tests and constrained job `wrapper-test-20260722-090001` pass. Merge the public commit into Sync and remove its duplicate source flow before another Chromium attempt. |

Do not close an issue based only on patch metadata lint or overlay injection.
